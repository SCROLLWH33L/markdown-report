% \ProvidesPackage{preamble}

% \def\first{John}
% \def\last{Doe}
% \def\email{student@youremailaddress.com}

% \renewcommand*\contentsname{Table of Contents}
% \newcommand{\code}{\verb}

% \usepackage{graphicx}
% \graphicspath{ {./images/} }

% \usepackage{hyperref}
% \usepackage{xcolor}

\usepackage{fancyhdr}
\fancyhead{}
\fancyfoot{}
\fancyfoot[R]{\thepage{} $\vert$ Page}
\pagestyle{fancy}
\renewcommand{\headrulewidth}{0.0pt}
\renewcommand{\footrulewidth}{0.4pt}

% backgroundcolor
% commentstyle
% keywordstyle
% numberstyle
% stringstyle
% basicstyle

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\usepackage{listings}
\lstdefinestyle{default}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily,
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false,
    upquote=true,
    frame=lines
}
\lstset{style=default}
\lstdefinestyle{small}{basicstyle=\ttfamily\small}
\lstdefinestyle{smaller}{basicstyle=\ttfamily\footnotesize}
\lstdefinestyle{smallest}{basicstyle=\ttfamily\scriptsize}

% Override default figure placement To be within the flow of the text rather
% than on it's own page.
\usepackage{float}
\makeatletter
\def\fps@figure{H} 
\makeatother

\pdfgentounicode=1

% \usepackage{parskip}
% \setlength\parskip{\the\baselineskip}
% \setlength\parindent{0pt}

% \tolerance=1
% \emergencystretch=\maxdimen
% \hyphenpenalty=10000
% \hbadness=10000

